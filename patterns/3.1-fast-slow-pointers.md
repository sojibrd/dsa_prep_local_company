### 3.1 Fast & Slow Pointers

**চিনবেন কীভাবে:** "detect cycle", "middle of list", "nth from end", "intersection" — দুই pointer ভিন্ন গতিতে/ব্যবধানে

**Demo: Linked List Cycle II** — [LC 142](https://leetcode.com/problems/linked-list-cycle-ii/) _(Medium — Amazon/Meta/Google High)_

**Statement (Demo):** linked list-এর `head` দেওয়া — cycle থাকলে cycle শুরু হওয়ার নোড return করুন, না থাকলে `null`। O(1) space-এ করতে হবে।
উদাহরণ: `head=[3,2,0,-4]`, tail connects to index 1 → node with value `2` | ⚡ `n ≤ 10⁴`

**Approach:** fast ২ ধাপ, slow ১ ধাপ — দেখা হলে cycle আছে। এরপর একটা pointer head-এ ফিরিয়ে দুটোই ১ ধাপ করে চালান; আবার যেখানে মিলবে সেটাই cycle-এর শুরু (গণিত: head→start দূরত্ব = meeting→start দূরত্ব mod cycle)।

```js
function detectCycle(head) {
  let slow = head,
    fast = head;
  while (fast && fast.next) {
    slow = slow.next;
    fast = fast.next.next;
    if (slow === fast) {
      // cycle পাওয়া গেছে
      let p = head;
      while (p !== slow) {
        p = p.next;
        slow = slow.next;
      }
      return p; // cycle-এর শুরুর নোড
    }
  }
  return null;
}
```

**Complexity:** Time O(n), Space O(1)

**প্রবলেম লিস্ট:**

- [x] **Linked List Cycle** — [LC 141](https://leetcode.com/problems/linked-list-cycle/) — 🔥 Must-do
      → Statement: linked list-এর `head` দেওয়া — list-এ cycle আছে কিনা `true`/`false` return করুন। (cycle = কোনো নোডের `next` আগের কোনো নোডে পয়েন্ট করে।)
      উদাহরণ: `head=[3,2,0,-4]`, tail connects to index 1 → `true` | ⚡ `n ≤ 10⁴`
      → আমার সমাধান: fast/slow pointer — fast ২ ধাপ, slow ১ ধাপ। একসময় দেখা হলে cycle আছে, fast null হলে নেই।
      → যে সমস্যা হয়েছিল: `fast && fast.next` দুটো চেক করা না হলে NullPointerException।
- [x] **Middle of the Linked List** — [LC 876](https://leetcode.com/problems/middle-of-the-linked-list/) — ⚪ Bonus
      → Statement: singly linked list-এর `head` দেওয়া — middle node return করুন। দুটো middle থাকলে দ্বিতীয়টা return করুন।
      উদাহরণ: `[1,2,3,4,5]` → node `3` | ⚡ `n ≤ 100`
      → আমার সমাধান: fast/slow pointer — fast শেষে গেলে slow মাঝখানে। জোড় সংখ্যায় দ্বিতীয় মধ্য নোড পাওয়া যায়।
      → যে সমস্যা হয়েছিল: `while (fast && fast.next)` — fast.next চেক না করলে even-length list-এ null access।
- [x] **Remove Nth Node From End of List** — [LC 19](https://leetcode.com/problems/remove-nth-node-from-end-of-list/) — 🔥 Must-do _(n ব্যবধানে দুই pointer + dummy)_
      → Statement: linked list-এর `head` ও integer `n` দেওয়া — শেষ থেকে `n`-তম নোড সরিয়ে দিন। modified list-এর head return করুন। one pass-এ করতে হবে।
      উদাহরণ: `[1,2,3,4,5], n=2` → `[1,2,3,5]` | ⚡ `n ≤ 30`
      → আমার সমাধান: dummy node + দুই pointer। fast কে n+1 ধাপ এগিয়ে দিয়ে তারপর দুটো একসাথে চালালে fast null হলে slow nth-এর আগে থাকে — সেখানে cut করুন।
      → যে সমস্যা হয়েছিল: head-ই remove হতে পারে সে ক্ষেত্রে dummy ছাড়া সামলানো কঠিন। n+1 (not n) ধাপ দেওয়া।
